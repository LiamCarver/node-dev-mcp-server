{
  "name": "mcp-file-server",
  "version": "1.0.0",
  "description": "A basic MCP server for file operations",
  "type": "module",
  "bin": {
    "mcp-file-server": "./build/server.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node build/server.js",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
